using System;

namespace AwesomePortal
{
    internal class LoginHelper
    {
        internal static bool login(string username, string password)
        {
            // Gửi lên server và nhận kết quả
            return true;
        }
    }
}